

function problem2(inventory){
    for(let i=0;i<inventory.length;i++){
    
    console.log("last car is "+inventory[inventory.length-1].car_make+" "+inventory[inventory.length-1].car_model);
}}
module.exports = {problem2}